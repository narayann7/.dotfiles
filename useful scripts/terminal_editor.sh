#! /bin/bash
cat > text.txt
# cat >>text.txt //it will apend the new text to exitsing one above command will replace the file

#run the script and type any thing , use ctrl + d to quit the input will store into the text file